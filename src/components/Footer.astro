---
import type { Locale } from "../i18n/config";
import { localizedPath } from "../i18n/config";
import { ui } from "../i18n/ui";
import { links } from "../data/links";

interface Props { brand?: "whitzard" | "nuwa"; locale: Locale; }
const { locale } = Astro.props;
const copy = ui[locale];
---

<footer class="site-footer">
  <div class="site-container site-footer__top">
    <div class="site-footer__brand">
      <a href={localizedPath("/", locale)} class="wordmark wordmark--light" aria-label="Whitzard 白泽">
        <img class="wordmark__logo wordmark__logo--footer" src="/assets/logo/whitzard_logo.png" width="48" height="48" alt="" aria-hidden="true" loading="lazy" decoding="async" />
        <span class="wordmark__text">Whitzard</span><span class="wordmark__han">白泽</span>
      </a>
      <p>{copy.footer.positioning}</p>
      <p class="site-footer__research">{copy.footer.researchLine}</p>
    </div>
    <div class="site-footer__column">
      <p class="site-footer__label">{copy.footer.platform}</p>
      <a href={localizedPath("/agentguard", locale)}>AgentGuard</a>
      <a href={localizedPath("/solutions", locale)}>{locale === "zh" ? "应用场景" : "Use Cases"}</a>
      <a href={localizedPath("/contact", locale)}>{locale === "zh" ? "预约演示" : "Book a Demo"}</a>
    </div>
    <div class="site-footer__column">
      <p class="site-footer__label">{copy.footer.resources}</p>
      <a href={localizedPath("/nuwa", locale)}>{locale === "zh" ? "女娲实验室" : "NUWA Lab"}</a>
      <a href={localizedPath("/research", locale)}>{locale === "zh" ? "研究成果" : "Research"}</a>
      <a href={localizedPath("/open-ecosystem", locale)}>WhitzardAgent</a>
    </div>
    <div class="site-footer__column">
      <p class="site-footer__label">{copy.footer.company}</p>
      <a href={localizedPath("/about", locale)}>{locale === "zh" ? "关于我们" : "About"}</a>
      <a href={`mailto:${links.email}`}>{links.email}</a>
      <a href={links.github} target="_blank" rel="noreferrer">GitHub ↗</a>
      <a href={links.huggingface} target="_blank" rel="noreferrer">Hugging Face ↗</a>
    </div>
  </div>
  <div class="site-container site-footer__bottom">
    <p>© {new Date().getFullYear()} Whitzard</p><p>{copy.footer.legalLine}</p>
  </div>
</footer>
