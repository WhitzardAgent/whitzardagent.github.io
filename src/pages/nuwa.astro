---
import BaseLayout from "../layouts/BaseLayout.astro";
import { links } from "../data/links";
import { researchAssets } from "../data/generated/researchAssets";
import { newsItems, getNewsLink } from "../data/news";
import UnifiedResearchCard from "../components/UnifiedResearchCard.astro";
import ResearchAreaCard from "../components/ResearchAreaCard.astro";
import BriefCard from "../components/BriefCard.astro";
import NewsCard from "../components/NewsCard.astro";
import SectionHeader from "../components/SectionHeader.astro";
import CalloutPanel from "../components/CalloutPanel.astro";
import NuwaHero from "../components/NuwaHero";
import FiveStoneMotif from "../components/visuals/FiveStoneMotif.astro";

const featuredPapers = researchAssets.filter((p) => p.featured).slice(0, 4);
const recentPapers = researchAssets.slice(0, 5);

const researchThemes = [
  { title: "Autonomy Risks", tag: "Autonomy", description: "Self-replication, self-proliferation, and autonomous resource acquisition in frontier AI." },
  { title: "Deception", tag: "Deception", description: "AI deception in human-AI interaction, developer-facing deception, and trust dynamics." },
  { title: "Scheming", tag: "Scheming", description: "Evaluation faking, situational awareness, and observer effects in safety evaluations." },
  { title: "Loss-of-Control", tag: "Loss-of-Control", description: "Conditions under which AI systems may evade oversight or resist shutdown." },
  { title: "Agent Safety", tag: "Agent Safety", description: "Behavioral safety, thought correction, and guardrails for AI agent systems." },
  { title: "Evaluation", tag: "Evaluation", description: "Executable risk-evaluation environments, benchmarks, and evaluation integrity." },
  { title: "Governance Evidence", tag: "Governance", description: "Technical evidence supporting responsible AI governance and public accountability." },
];

const transparentCards = [
  {
    title: "Transparent",
    desc: "Public research notes, open evaluation assumptions, and clear evidence standards.",
  },
  {
    title: "Third-Party",
    desc: "Independent safety evaluation and governance evidence for frontier AI systems.",
  },
  {
    title: "Open Infrastructure",
    desc: "Benchmarks, tools, datasets, and frameworks that support broader AI safety research.",
  },
  {
    title: "Governance-Oriented",
    desc: "Technical evidence that can support responsible AI governance and public accountability.",
  },
];

// Research-related news
const nuwaNews = newsItems.filter(
  (n) => n.type === "Research" || n.type === "Event" || n.type === "Update"
);
---

<BaseLayout
  title="Nuwa Frontier AI Safety Lab"
  description="Nuwa builds transparent, third-party, open infrastructure and benchmarks for frontier AI safety evaluation and governance."
  brand="nuwa"
  ogImage="/assets/og/nuwa-og.png"
>
  <!-- Animated Hero -->
  <NuwaHero client:load />

  <!-- Separator -->
  <FiveStoneMotif layout="row" size={5} gap={10} />

  <!-- Transparent Third-Party Evaluation -->
  <section class="py-16 md:py-24 bg-nw-warm-paper">
    <div class="max-w-[1120px] mx-auto px-6">
      <SectionHeader title="Transparent Third-Party Evaluation" subtitle="Nuwa's approach to independent frontier AI safety evaluation and governance evidence." />
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4">
        {transparentCards.map((card) => (
          <div class="card card--editorial hover-lift">
            <h3 class="text-nw-ink font-semibold text-sm mb-2">{card.title}</h3>
            <p class="text-nw-muted text-sm leading-relaxed">{card.desc}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Research Themes -->
  <section class="py-16 md:py-24">
    <div class="max-w-[1120px] mx-auto px-6">
      <SectionHeader title="Research Themes" subtitle="Core themes driving our frontier AI safety research." />
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
        {researchThemes.map((theme) => (
          <ResearchAreaCard title={theme.title} tag={theme.tag} description={theme.description} />
        ))}
      </div>
    </div>
  </section>

  <!-- Featured Research -->
  <section class="py-16 md:py-24 bg-nw-mist">
    <div class="max-w-[1120px] mx-auto px-6">
      <SectionHeader title="Featured Research" />
      <div class="space-y-5">
        {featuredPapers.map((paper) => (
          <UnifiedResearchCard
            title={paper.title}
            authors={paper.authors}
            year={paper.year}
            venue={paper.venue}
            status={paper.status}
            summary={paper.summary}
            url={paper.url}
            pdfUrl={paper.pdfUrl}
            topic={paper.topic}
            featured={paper.featured}
            brand="nuwa"
          />
        ))}
      </div>
    </div>
  </section>

  <!-- News -->
  {nuwaNews.length > 0 && (
    <section class="py-16 md:py-24">
      <div class="max-w-[1120px] mx-auto px-6">
        <div class="flex items-center justify-between mb-8">
          <h2 class="text-nw-ink font-semibold" style="font-size: var(--text-h2);">News</h2>
          <a href="/news" class="text-link text-sm font-medium" style="color: var(--nw-jade);">
            View all <span class="arrow">&rarr;</span>
          </a>
        </div>
        <div class="space-y-4">
          {nuwaNews.map((item) => (
            <NewsCard
              title={item.title}
              date={item.date}
              type={item.type}
              summary={item.summary}
              href={getNewsLink(item)}
              source={item.source}
              featured={item.featured}
            />
          ))}
        </div>
      </div>
    </section>
  )}

  <!-- Latest Nuwa Brief -->
  <section class="py-16 md:py-24 bg-nw-warm-paper">
    <div class="max-w-[1120px] mx-auto px-6">
      <SectionHeader title="Latest Nuwa Brief" />
      <BriefCard
        title="The Science of Frontier AI Risk Evaluation"
        date="2026"
        url="https://nuwasafety.substack.com/p/science-of-frontier-ai-risk-evaluation"
        summary="Nuwa essay on making frontier AI risk evaluation more scientific, evidence-based, and operational."
        external={true}
      />
    </div>
  </section>

  <!-- Publications Preview -->
  <section class="py-16 md:py-24 bg-nw-mist">
    <div class="max-w-[1120px] mx-auto px-6">
      <div class="flex items-center justify-between mb-8">
        <h2 class="text-nw-ink font-semibold" style="font-size: var(--text-h2);">Publications &amp; Papers</h2>
        <a href="/research" class="text-link text-sm font-medium" style="color: var(--nw-jade);">
          View all <span class="arrow">&rarr;</span>
        </a>
      </div>
      <div class="space-y-4">
        {recentPapers.map((paper) => (
          <UnifiedResearchCard
            title={paper.title}
            authors={paper.authors}
            year={paper.year}
            venue={paper.venue}
            status={paper.status}
            summary={paper.summary}
            url={paper.url}
            pdfUrl={paper.pdfUrl}
            topic={paper.topic}
            featured={paper.featured}
            brand="nuwa"
          />
        ))}
      </div>
    </div>
  </section>

  <!-- Follow / Collaborate -->
  <section class="py-16 md:py-24">
    <div class="max-w-[1120px] mx-auto px-6">
      <div class="grid grid-cols-1 md:grid-cols-2 gap-5">
        <CalloutPanel variant="nuwa" align="left" description="Follow Nuwa's research updates on frontier AI risk, agent safety, and controllable AI.">
          <div class="flex flex-wrap gap-3">
            <a href={links.substack} target="_blank" rel="noopener noreferrer" class="btn btn--primary">Subscribe to Nuwa Brief</a>
            <a href={links.x} target="_blank" rel="noopener noreferrer" class="btn btn--secondary">Follow on X</a>
          </div>
        </CalloutPanel>

        <CalloutPanel variant="default" align="left" description="We collaborate on frontier AI safety evaluation, agent safety research, and runtime defense.">
          <div class="flex flex-wrap gap-3">
            <a href="/contact" class="btn btn--primary">Contact Us</a>
            <a href={links.github} target="_blank" rel="noopener noreferrer" class="btn btn--secondary">GitHub</a>
          </div>
        </CalloutPanel>
      </div>
    </div>
  </section>
</BaseLayout>
